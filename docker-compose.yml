version: '3.8'

services:
 web:
    build: .
    container_name: web
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt
      - ./site:/usr/share/nginx/html
      - /home/ubuntu/uno/frontend/dist:/usr/share/nginx/html/uno/frontend

